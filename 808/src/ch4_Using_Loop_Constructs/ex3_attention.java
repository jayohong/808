package ch4_Using_Loop_Constructs;

public class ex3_attention {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}

/*
 
 Given the for loop construct:

for ( expr1 ; expr2 ; expr3 ) {
    statement;
}
Which two statements are true?

A. This is not the only valid for loop construct; there exits another form of for loop constructor.
B. The expression expr1 is optional. it initializes the loop and is evaluated once, as the loop begin.
C. When expr2 evaluates to false, the loop terminates. It is evaluated only after each iteration through the loop.
D. The expression expr3 must be present. It is evaluated after each iteration through the loop.
 
 
 
Ans is AB
C錯在 判斷是在進入回圈前而不是而不是之後
D錯在 expr3並不一需要
 
哪兩個陳述是正確的？
A. 這不是唯一有效的 for 循環結構； 存在另一種形式的 for 循環構造函數。
B. 表達式 expr1 是可選的。 它初始化循環並在循環開始時評估一次。
C. 當 expr2 評估為假時，循環終止。 它僅在循環的每次迭代後進行評估。
D. 表達式 expr3 必須存在。 它在循環的每次迭代後進行評估。
 */
